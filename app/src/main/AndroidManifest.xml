<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ashis.lenovo.androidinterview">

    <application
        android:allowBackup="true"
        android:icon="@drawable/lgg"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:name=".splashscreen"


            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Simple_questions" />
        <activity android:name=".Tough_questions" />
        <activity android:name=".seeothersapp" />
        <activity android:name=".FrontPage"/>
    </application>

</manifest>